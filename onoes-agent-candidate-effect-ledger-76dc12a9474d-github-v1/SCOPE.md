# Locked scope

onoes-agent-candidate-effect-ledger-76dc12a9474d-github-v1
Publisher source: 76dc12a9474d626efcf1944c86f4ffd131bfef2f.

Eleven exact original product Git blobs: two ledger/state modules, two direct
local dependencies, the complete focused test suite, both helpers, the decision
document, lockfile and tsconfigs. All product source bytes are unchanged.

source/<productPath> holds source/docs/configs; tests/<productPath> holds tests.
For dependency reasoning, reconstruct paths via SOURCE_IDENTITIES.productPath.
The intentional tests/tests prefix does not describe a runnable package. No
execution or installation is authorized. All scoped local imports are supplied;
external dependencies and runtime semantics remain outside this extract.

source/context/ holds two installed better-sqlite3 context files, NOT product
blobs. The focused TAP and other receipts are producer evidence, not independent
execution. No full project, secrets, private logs/paths, VM IDs/images, binaries,
accounts, approval keys, signing, enrollment, provider, HTTP/UI or consumers.

Read AUDIT_REQUEST.md. Return a separate report to the operator, not a GitHub edit.
This packet does not grant any execution or production permission. Removing it
from the latest public tree after closure does not erase Git history.
