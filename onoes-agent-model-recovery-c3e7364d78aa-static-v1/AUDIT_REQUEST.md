# UNIVERSAL AUDIT REQUEST — Onoes-Agent model recovery and paired read

> **AUDIT-ONLY DOCTRINE — READ FIRST:** Read-only, no-cost STATIC_SOURCE audit. Do not branch, build, edit, commit, push, run workflows, call providers/models, use hosted runners, deploy, or incur API/compute charges. Inspect the supplied bytes and return Markdown only. Never request secrets or improvise missing evidence.

## Audit identity

- Audit ID: `onoes-agent-model-recovery-c3e7364d78aa-static-v1`
- Project: Onoes-Agent Windows model planning, allocation and recovery.
- Auditor/model and route/provider: pending operator confirmation; state actual identity and prior involvement.
- Type: security, implementation, evidence, architecture and regression.
- Requested by: Onoes; packet prepared by implementation author, 2026-09-09.
- Review time: reviewer supplies actual UTC timestamp, not this preparation date.
- Exact publisher source: `c3e7364d78aaf9a5d35b11cb3456d77e0bb976d2`.
- Evidence requested: STATIC_SOURCE, never EXACT_TREE or execution acceptance.

## Scope lock

Packet path: `onoes-agent-model-recovery-c3e7364d78aa-static-v1/`.
`receipts/SOURCE_IDENTITIES.json` defines the exact 52 source/test/config members.
There are 58 manifest-covered members and 59 total files including the manifest.
Members are raw Git blob bytes, not browser-rendered or normalized copies.
`receipts/DEPENDENCIES.json` lists lexical imports and missing context; it is not
runtime closure. Executor/Builder-barrel files are caller/type context only.
Name missing load-bearing files rather than assuming their behavior.

Primary reading order:

1. `source/src/build-only/windows-managed-model-budget.ts`: immutable funding,
   reserve/outcome, exact read-back, recovery inventory and row-count guard.
2. `source/src/build-only/windows-managed-model-runner.ts`: consent data binding,
   one attempt, ambiguous settlement, failure observations and poison lifecycle.
3. `source/src/build-only/windows-operator-model-recovery.ts` and
   `windows-operator-session.ts`: session/book pin, explicit metadata read, quotas
   and disclosure rechecks. This controller is not connected to HTTP or Studio.
4. Suggestion/draft planning, source inspection, policy and canonical schemas.
5. `source/src/model/openai-responses-adapter.ts`: HTTPS/loopback and redirects,
   raw/text bounds, generic host retry versus the managed single-attempt limit.
6. Tests under `tests/tests/unit/`; source read before producer receipts.

Excluded: private Git bundle/full repository, installed dependency implementations,
compiled tests, package artifacts, prior reviewer reports, real provider identity,
billing, source-transfer/spending approval, HTTP/Studio production wiring, native
filesystem/process confinement, guests, privileged installer and Rust authority.
The extract cannot reproduce the complete project. No consumer activation is
implied by the presence of its dormant parser/controller or a test that calls it.

## Cost and mutation controls

- No GitHub Actions or hosted runners; no installs, builds or subject execution.
- No subject tests, SQLite fixtures, child processes or provider calls. The root
  template's generic permission for safe offline tests does not apply here.
- Only ordinary public GitHub reads and bounded offline hash/text analysis.
- No repo writes, PRs, branches, commits, upload, deployment, credentials or charges.
- Return the report separately; do not post it into the public packet yourself.

## Audit questions

1. Can any source/response be released after request, policy, scope, profile or
   session expiry/revocation? Trace both runner and recovery-controller paths.
2. Does uncertain reserve/commit/read-back ever trigger a provider or follow-up
   terminal write? Separate attempted callback from verified persisted state.
3. Can terminal response loss cause a second settlement, refund, retry, or a false
   successful receipt? Challenge the one-row guard and exact intended read-back.
4. Does local failure metadata overstate dispatch, remote completion, charge or
   durability? Are null contexts and late completion described honestly?
5. Can recording failure erase the original bounded reason, leak raw diagnostics,
   or reset poison? Check temporary busy versus permanent poison and explicit close.
6. Does recovery inventory discover retained allocations without remembered IDs,
   use one consistent WAL snapshot, preserve full allowances, and remain bounded?
7. Can the paired controller read a book before session+CSRF verification, accept
   another book/path, or disclose after session expiry, revocation or host close?
8. Does rate accounting cover malformed authenticated requests, failed reads and
   sliding-window boundaries? Can controller lifecycle ownership undermine it?
9. Are byte caps applied before parse and before disclosure? Distinguish trusted
   host snapshot seams from external request data and identify overclaims.
10. Does the request's explicit read acknowledgement actually imply human consent?
    Do not conflate possession of the launcher secret with authenticated identity.
11. Does remote plaintext deny while canonical loopback remains intentional? Does
    redirect error mode prevent destination substitution with both auth/API modes?
12. Does the managed runner force one attempt despite generic host retry settings?
    Do not equate direct generic extraction use with the bound runner's behavior.
13. Are request budgets based on the bytes actually represented twice/separately
    in the provider request? Distinguish structural caps from promised capacity.
14. Are canonical historical funding/reservation/outcome/profile bytes changed?
    Can a changed funding limit legitimize history reset or hide retained attempts?
15. Which tests are genuinely falsifiable and which use trusted fake adapters?
    What real Windows, concurrency, power-loss or provider evidence is missing?
16. What minimal changes or additional context are needed before an opted-in paired
    HTTP read consumer can be reviewed? No approval for production wiring is sought.

Known residuals to evaluate, not mechanically close: `outcome-unknown` can include
a no-dispatch local attempt; no automatic refunds; no response persistence; actual
charges/prices are unverified; full-scan write latency is not a passed benchmark;
host book custody and controller lifetime are trusted; session delivery is missing;
no production routes, UI, arbitrary-verifier confinement or installer acceptance.

## Required evidence method

First acknowledge the separately supplied expected MANIFEST.sha256 file SHA-256.
If absent, state delivery incomplete; compute internal consistency if possible but
do not invent an independent trust root or infer it from a filename. Verify the
58 listed hashes, exact member-set equality, byte lengths and `blob <length>\0`
Git blob OIDs when bounded hashing is available. Describe inability to access raw
bytes honestly. The manifest cannot hash/authenticate itself.

Publisher commit membership is not independently established by hashing extracted
files. `receipts/PRODUCER_EVIDENCE.json` is an author summary, not raw execution or
a signed receipt. Its package identity names parent code checkpoint `361c3eeb`;
the audited `c3e7364d` adds only documentation. No package is supplied. No native,
installed or provider success follows from its counts.

Separate inspected code facts, reproduced hash arithmetic, proposed probes,
assumptions and author-reported tests. Cite exact supplied path/symbol/line. For
findings propose a minimal falsifiable test and explain trust assumptions; do not
run the subject. Preserve identifiers exactly. Redact sensitive values if found.

## Required wiki-ready report

Return one separate Markdown report with these exact sections:

1. Executive Summary — scope and whether advancement is blocked.
2. Audit Identity — audit ID, project, actual model/route, type, UTC and revision.
3. Scope and Method — files inspected, exclusions, commands run and cost controls.
4. Verdict — implementation, security, evidence and scoped advancement.
5. Blocking Findings — ID, severity, file/symbol/line, observed fact, consequence,
   proposed probe, required correction and new/known/resolved-but-unverified status.
6. Nonblocking Findings — same fields. No findings only when scope supports it.
7. Verification Results — hashes, manifest, receipts, scope and unexpected failures;
   subject tests NOT RUN. Never describe a proposed probe as a passing one.
8. Security and Integrity Review — secrets, injection, authorization, isolation,
   mutation, concurrency, provenance, replay/rollback and fail-closed behavior.
9. Limitations and Missing Evidence — explicit untested platforms and boundaries.
10. Required Next Action — smallest ordered corrections/review/evidence needs.
11. Explicit Non-Claims — not production, installed, live-provider or release approval.

Allowed verdict vocabulary:

```text
IMPLEMENTATION_VERDICT: PASS | PASS_WITH_FINDINGS | NEEDS_CHANGES | NOT_IMPLEMENTED
SECURITY_VERDICT: PASS | NEEDS_REVIEW | BLOCKED | NOT_APPLICABLE
EVIDENCE_VERDICT: COMPLETE | PARTIAL | INSUFFICIENT
ADVANCEMENT: YES | NO (dormant implementation only; never production activation)
```

Finish with:

```text
MODEL_ID:
ROUTE:
AUDITED_REVISION:
IMPLEMENTATION_VERDICT:
SECURITY_VERDICT:
EVIDENCE_VERDICT:
BLOCKING_FINDINGS:
NONBLOCKING_FINDINGS:
TEST_RESULTS: NOT RUN — STATIC_SOURCE scope
SCOPE_RESULT:
PLATFORM_LIMITATIONS:
ADVANCEMENT:
NEXT_REQUIRED_ACTION:
```

Neither a static PASS nor a missing finding closes the independent executed,
Windows-native, authenticated consumer or operator acceptance gates.
