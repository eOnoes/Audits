# Scope lock

Audit ID: onoes-agent-model-recovery-c3e7364d78aa-static-v1
Publisher source: c3e7364d78aaf9a5d35b11cb3456d77e0bb976d2

STATIC_SOURCE only. Exact raw Git member inventory is receipts/SOURCE_IDENTITIES.json.
No bundle, compiled files, installed dependencies, package or independent commit-membership
proof is supplied. The producer reads its own Git object store. The tests/tests prefix
preserves product paths. Dependencies are lexical analysis, not runtime closure.

Primary: managed model runner, allocation journal, recovery inventory/failure observations,
paired operator recovery read and process session, provider transport. Planning, policy,
proposal and schema context is included. Executor and Builder barrel are context only;
missing dependencies are explicitly listed, not assumed audited. No HTTP/Studio/native/
installer consumer, actual provider, billing or production authorization is in scope.

Delivery: LOCAL_PENDING_TRANSFER_APPROVAL. Recipient pending operator confirmation.
The generator does not publish or call a reviewer. Expected MANIFEST.sha256 file hash
must be delivered separately and acknowledged, never inferred from itself or a filename.
Public Git history is persistent even after latest-tree cleanup. No report exists yet.
