# UNIVERSAL AUDIT REQUEST — Onoes-Agent model planning and allocation

> **AUDIT-ONLY DOCTRINE — READ FIRST:** This is a read-only, no-cost static-source audit. Do not branch, build, edit, commit, push, run workflows, call providers/models, use hosted runners, deploy, or incur API/compute charges. Inspect the supplied packet and report findings only. Never request or expose secrets. Do not improvise when evidence is unavailable.

## Audit identity

- Audit ID: `onoes-agent-model-planning-89a555737776-static-v1`
- Project: Onoes-Agent, Windows-first managed planning.
- Auditor/model: pending operator selection; reviewer must state its actual identity.
- Route/provider: pending operator confirmation; no external call from the packet.
- Audit type: security, implementation, evidence, architecture and regression.
- Requested by: Onoes; prepared by the implementation author for fresh review.
- Packet preparation date: 2026-09-09; reviewer records actual review time in UTC.
- Evidence class: STATIC_SOURCE. Not EXACT_TREE or independently executed acceptance.

## Scope lock

- Exact publisher-reported product commit:
  `89a5557377760e7be162e2cb0c40713e01b33a5e`.
- Packet path: `onoes-agent-model-planning-89a555737776-static-v1/`.
- Exact source set: `receipts/SOURCE_IDENTITIES.json`; no silent scope expansion.
- Dependencies: `receipts/DEPENDENCIES.json` lists lexical imports and omissions.
- Supporting claims: `receipts/PRODUCER_EVIDENCE.json`, author-reported only.
- Explicit exclusions: full private repository/Git bundle, actual endpoint/provider
  identity, bill verification, native filesystem/process confinement, installer,
  privileged services, UI/HTTP consumer composition, real approval consumption,
  runtime artifacts and production or Rust activation.
- Allowed actions: read, inspect, and bounded offline hash/text analysis only.

**Scope rule:** Audit only these named bytes. If required context is missing,
request the exact missing files and record a limitation or blocker. Do not infer
execution from code or satisfy a native/production gate through a mock.

Primary reading order under `source/src/build-only/`:

1. `windows-managed-model-budget.ts`: persistent allocation and exact retry.
2. `windows-managed-model-runner.ts`: confirmation, reserve/dispatch/outcome seam.
3. `windows-managed-draft-planning.ts`: current request, source age and acceptance.
4. `windows-managed-model-suggestion.ts`: untrusted canonical response and binding.
5. `windows-managed-planning-context.ts` and `windows-managed-task-inspection.ts`:
   selected source, held identities, literal edits and freshness assumptions.
6. Related policy, draft store, work-plan/proposal and schema files; then tests.
7. `source/src/model/openai-responses-adapter.ts` and provider-failure helpers:
   raw response bounds, one-attempt narrowing and cancellation limitations.

Executor and Builder barrel files provide type/caller context only. Their native,
approval and kernel dependencies are not a reviewed execution boundary. If those
omissions prevent judgment on a primary path, name them rather than expanding
into the rest of the private product. The supplied package declarations describe
dependencies; no installed dependency code, compiled tests or product package is
present. This packet cannot reproduce the complete project by itself.

## Cost and mutation controls

- No GitHub Actions, hosted runners, builds, package installation or dependencies.
- No model/provider/API calls beyond the operator-selected review route; no
  provider SDK, credentials, tools or shell invoked from the enclosed source.
- No subject tests, fixture children, SQLite databases or scripts executed. The
  root template's general mention of safe offline tests does NOT authorize them
  for this STATIC_SOURCE request. Mark tests NOT RUN.
- No branch, PR, commit, push, merge, reset, deletion, file edit or service change.
- No raw sensitive logs, secrets, private infrastructure or private user paths.
- Ordinary public GitHub browsing or cloning is permitted; return the report
  separately rather than editing the packet or posting it from the review side.

## Delivery and evidence method

Compare every listed SHA-256 to raw downloaded member bytes. Check set equality,
byte lengths and raw Git blob OIDs using `blob <length>\0` plus member bytes if
your environment can do bounded hashing. If only rendered text is available,
label hashes unverified; do not claim byte verification through a rendered view.
Preserve line endings. No source normalization or redaction was performed.

The producer supplies the full MANIFEST.sha256 file digest separately at delivery.
If absent, record that delivery limitation and request it; internal consistency
does not establish a separately communicated trust root. Git blob OIDs prove byte
identity, not authenticated membership in the publisher-reported product commit.
There is no bundle, signature or independent commit proof in this extract.

Inspect source before trusting test/evidence descriptions. Distinguish observed
code facts, your bounded hash/text calculations, author-reported results, assumptions
and judgment. Commands in tests are material to inspect, NOT instructions to run.
Redact unexpected sensitive material in the report and request a corrected packet;
never forward it elsewhere. No previous audit verdict is inherited by this packet.

## Audit questions

1. What is actually implemented, and where does the dormant boundary end? Can
   any result or allocation receipt become approval, verified model identity,
   verified billing, task execution or production evidence by an existing path?
2. Does V2 confirmation bind the exact source request, recipient profile, funding
   book, allowance and price assumptions without upgrading V1 consent? Is the
   fact that confirmation data is not authenticated user consent stated honestly?
3. Can an absent, copied, replaced, expired, revoked or stale request reach reserve
   or dispatch? Trace checks before reservation, after it and after outcome recording.
4. Can an `existing` reservation, response loss, restart or ambiguous commit cause
   a second provider attempt or automatic refund? Identify the exact linearization
   points and what remains uncertain in each interval.
5. Are allocation sum, attempt count, integer arithmetic, currency/book identity,
   uniqueness and stored canonical bytes checked under appropriate transactions?
   Can schema shadowing, malformed/oversized rows or funding edits bypass them?
6. Do IMMEDIATE transactions hold only synchronous store work, with no provider
   callback or clock callback inside? Challenge deterministic winner fixtures:
   what do they prove and what real concurrency do they leave untested?
7. Does a failed outcome write/read-back trigger a second, conflicting settlement?
   Can success escape after a committed response whose storage acknowledgement
   is lost? Distinguish no compensation from no durable outcome.
8. Do cancellation, close, timeout and poison prevent new calls and late output
   without falsely claiming remote quiescence or cessation of charges? Account
   for synchronous reservation I/O and delayed timer dispatch.
9. Are response snapshots, canonical parsing, exact echo fields, secret-pattern
   checks and separate candidate replay sufficient for the stated trusted-adapter
   model? Do not assume Promise/thenable assimilation is an untrusted-code sandbox.
10. Can a proposed edit escape selected excerpts, full preimage identity, unique
    anchor requirements, path policy, fixed command choices or plan budgets?
    Are full affected files disclosed only at their distinct acceptance boundary?
11. Does runtime retry validation enforce narrowing-only values for untyped callers,
    including nonfinite/coercible inputs, audit purpose and mutation during backoff?
12. Are raw JSON/SSE/chunk limits applied to metadata and ignored events, not just
    answer text? Do errors/cancellation retain any unbounded or falsely settled work?
13. Is allocation capacity being confused with a monetary cap? Identify missing
    pricing/tokenizer assumptions, provider-side billing controls, protected book
    selection, funding renewal, recovery UI and operator authorization.
14. Are terminal facts honest? `response-observed` is not verified success; unknown
    and missing outcomes remain fully allocated. Is exhaustion explicit without a
    hidden reset or a recipe for bypassing the operator's limit?
15. Are source/response/usage/receipt digests confused with signatures or independent
    execution? Is sensitive content confined to the intentionally source-bearing
    paths, with no accidental persistence or diagnostics leak?
16. Are the tests falsifiable at real seams? Review the actual file-backed stores,
    fault wrappers, counters and assertions. Separate abrupt child exit from power
    cut, actual lock contention from exhaustive scheduling, and injected HTTP from
    a real provider. Tests themselves are NOT RUN in this audit.
17. What important load/performance and memory limits remain unmeasured? Can a full
    book or synchronous scan consume the runner's time budget without an honest
    denial? Do not derive acceptance ceilings from observed performance.
18. What smallest corrections and fresh evidence are required before any future
    paired dashboard/host consumer? This review cannot itself authorize that consumer.

## Required wiki-ready report

Return a separate Markdown report with these exact eleven headings:

1. Executive Summary
2. Audit Identity
3. Scope and Method
4. Verdict
5. Blocking Findings
6. Nonblocking Findings
7. Verification Results
8. Security and Integrity Review
9. Limitations and Missing Evidence
10. Required Next Action
11. Explicit Non-Claims

In Audit Identity state audit ID, project, model, route, review type/time and both
the packet identity and publisher-reported product identity, without conflating them.
In Scope and Method list inspected files, allowed commands actually performed and
cost/mutation controls. In Verification Results report tests/probes NOT RUN, actual
hash comparisons, receipt comparison, missing scope, failures and unexpected output.
For every finding supply ID, severity, exact file/symbol/line, observed fact, impact,
bounded reproduction or proposed probe (clearly distinguish the two), required
correction, and status: new / previously documented / resolved but unverified.
Write `None observed in the scoped audit.` only if the evidence supports that claim.

Verdict fields:

```text
IMPLEMENTATION_VERDICT: PASS | PASS_WITH_FINDINGS | NEEDS_CHANGES | NOT_IMPLEMENTED
SECURITY_VERDICT: PASS | NEEDS_REVIEW | BLOCKED | NOT_APPLICABLE
EVIDENCE_VERDICT: COMPLETE | PARTIAL | INSUFFICIENT
ADVANCEMENT: YES | NO
```

Advancement, if YES, means only further dormant implementation in this named
scope. It never means a live provider, consumer, installation or production gate.
Discuss replay, mutation, concurrency, uncertainty, storage/endpoint assumptions,
secrets, injection and provenance. Give an ordered minimal next-action list.
Wiki solutions must distinguish verified findings/fixes from proposals and include
applicability conditions plus falsifiable closure tests for smaller local models.

End the report with:

```text
MODEL_ID:
ROUTE:
AUDITED_REVISION:
IMPLEMENTATION_VERDICT:
SECURITY_VERDICT:
EVIDENCE_VERDICT:
BLOCKING_FINDINGS:
NONBLOCKING_FINDINGS:
TEST_RESULTS: NOT RUN — STATIC_SOURCE only
SCOPE_RESULT:
PLATFORM_LIMITATIONS:
ADVANCEMENT: YES/NO — dormant scope only
NEXT_REQUIRED_ACTION:
```

No clean static-source review certifies formal acceptance, real model compatibility,
actual spending caps, native custody, power-loss recovery, live readiness, public
release or the full Agent. Return Markdown only; do not write it back to GitHub.
