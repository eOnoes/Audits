{
  "status": "configured tripwires pass",
  "method": "minimum exact source selection plus explicit JSON/helper root replacements",
  "excluded": [
    "credentials",
    "private logs",
    "VM images",
    "full project",
    "private paths",
    "runtime dependencies"
  ],
  "limitations": [
    "Pattern scan is not proof of absence of all secrets. No external transfer performed by this generator."
  ]
}
