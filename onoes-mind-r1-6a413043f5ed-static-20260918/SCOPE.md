# Scope — Onoes-Mind R1 static derivative

AUDIT ONLY: read-only subject; no execution, builds, installs, Actions, provider
calls, live agents, credentials, deployments or charges. Only completed sanitized
report delivery to eOnoes/Audits main at `onoes-mind-r1-6a413043f5ed-static-20260918/reports/AUDIT_REPORT.md` is allowed after workflow
checks. No branches, PRs, source changes or input-manifest regeneration. Preserve
reports with numbered addenda. Verify report readback and return immutable link;
if write access is absent, return full Markdown and BLOCKED_NO_GITHUB_WRITE_ACCESS.

Baseline project revision: `6a413043f5ed18fe1f8e27b446971775cb5fe39c`.
Candidate is uncommitted R1, never promoted. The packet commit is supplied by the
publisher separately and pins all public bytes. provenance.json maps original
archive-member hashes to published hashes. Public bytes use UTF-8/LF and replace
absolute local paths with X:/SANITIZED placeholders without changing line count.
Such placeholders can change runtime semantics: DO NOT RUN this derivative.

Included: every changed file, changed preimages where present, selected source
dependencies and migrations, relevant tests, v4.2 audit/reconciliation/plans,
sanitized producer receipt and test XML. Excluded: original ZIPs, Git objects,
databases, configs/auth files, private stores, unrelated historical reports and
most unchanged modules/tests. Original source manifest is not published as a
complete public tree; use provenance for this selected set only.

The reviewer cannot independently establish original Git preimages, local
working-tree identity, Windows tests, full transitive closure, full DC runtime
behavior or production security from this packet. Distinguish code findings,
documentation drift and evidence limitations. No full exact-tree pass is possible.

The operator reports no concurrent editors now; historical quality/ownership
closure and explicit promotion remain separate. New runtime/supervisor/exchange
candidates are excluded from this R1 audit. No changes to implementation, v4.1,
trial-agent files or production agent state were made to create this transport.
