# Current scope and honest evidence

Publisher source 7e923956dd014f44e8eb20449499de31c3e4d7f7, last externally reviewed product 5f974a28bf34e097713a17020e3b7b086102d22e. Prior Claude report allowed dormant coordinator/adapter implementation with synthetic ports only. M-1/M-2 model corrections, M-3 precision/testing and M-5 supplemental doc delta were made locally; current source/doc deltas are supplied, not silently attributed to the older report.

This packet contains actual build-only messages, validation-session, snapshot envelope and coordinator, plus synthetic ports exercising the real pure validator. No physical adapter or release consumer. Acks are exact consistency claims, not MACs or custody; same-port-object refusal is not store-wide fencing. All bootstrap history mismatches close, with no automatic one-ahead reconciliation. Lost response handling does not establish absence of effects.

Current producer focused 136/136, full 2131 tests/2129 pass/0 fail/2 skips. Source match is selected, not exact whole-tree: 23 TS files are byte-identical to current blobs; compiled files are hash-only evidence; 2 configs have explicit CRLF/LF derivation with exact executed copies. The full manifest/lock and runtime are excluded to avoid leaking the broader product; selected dependency pins state that limit. No independent tests run. The new coordinator suite has 22 cases, 53 positive family prefixes, 50 cancellation seam cuts and 16 settled deadline cuts. The older model and historical tests are not the actual coordinator.

No new capacity measurement is claimed: M-6 maximum-admissible bytes/resources and C12 remain OPEN. Prior probe/doc numbers are historical narrow-corpus claims. Real B-03/W1-W5, authenticated storage/owner/anchor/IPC, termination, installer, independent execution and operator acceptance remain mandatory. Previous report identity is same-family/different-session, not an independent second opinion; reviewer must disclose their own continuity. Historical missing chat-pin delivery remains a limitation, never retroactively fixed.
