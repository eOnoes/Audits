# Exact output

eOnoes/Audits main: onoes-agent-consumer-successor-fdf413aa0c7b-design-v2/reports/AUDIT_REPORT.md

Add only the sanitized report after workflow checks; no source/input/manifest
edits. Preserve existing reports with numbered addenda. Verify committed bytes
and return the immutable report URL. If unable, return full Markdown and
REPORT_DELIVERY: BLOCKED_NO_GITHUB_WRITE_ACCESS for operator delivery.
